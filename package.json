{
  "name": "isshin-phaser-game",
  "description": "Isshin — a Castlevania-like samurai homage built with Phaser 4",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config vite/config.dev.mjs",
    "build": "vite build --config vite/config.prod.mjs",
    "preview": "vite preview --config vite/config.prod.mjs --port 8080"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "terser": "^5.39.0",
    "typescript": "~5.7.2",
    "vite": "^6.3.1"
  }
}
